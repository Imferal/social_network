export type PostType = {
    id: number,
    message: string,
    likesCount: number,
    date: string,
}

export type DialogType = {
    id: number
    name: string
}

export type MessageType = {
    id: number
    message: string
    date: string
}